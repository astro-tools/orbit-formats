"""The conversion graph — routing through canonical forms.

Each format declares its preferred canonical form (Cartesian vs Keplerian; the frame
and time scale it expects); the graph routes a conversion through those forms rather
than enumerating every format pair.
"""
